# 哲唯科技~GEO交付平台 · 正式上线（B 方案）操作说明

目标：让网站 24 小时都能被任何人打开（不再依赖你这台电脑）。
你需要做的最核心的一件事：**注册 1 个免费账号**（GitHub 或 Cloudflare/Render），其余我已全部配置好。

## 路线一（推荐）：GitHub + Render 一键部署

### 第 1 步：注册 GitHub（免费）
1. 打开 https://github.com/signup 用邮箱注册（收验证邮件）。
2. 登录。

### 第 2 步：新建仓库并上传项目
1. 打开 https://github.com/new ，名称填 `zhewei-geo`，选 Public，Create。
2. 点 **uploading an existing file**。
3. 打开本文件夹 `C:\Users\DELL\Documents\Codex\哲唯科技GEO平台`，把除了 `node_modules`、`dist` 之外的所有文件和文件夹拖上去（`server\.env` 密钥文件不要传）。
4. Commit changes。

### 第 3 步：注册 Render 一键部署
1. 打开 https://render.com → Get Started → 用 GitHub 登录并授权。
2. **New** → **Blueprint** → 选 `zhewei-geo` 仓库。
3. Render 自动按 `render.yaml` 部署前端+后端（约 3-10 分钟）。

### 第 4 步：填密钥
1. Render 后台 → **zhewei-geo-backend** → **Environment** → 填 `LLM_API_KEY` = 你的 DeepSeek Key。
2. 保存 → Manual Deploy → Deploy latest commit。

### 第 5 步：连接前后端
1. 复制后端网址（形如 `https://zhewei-geo-backend.onrender.com`）。
2. **zhewei-geo-frontend** → Environment → `VITE_API_BASE` = `https://zhewei-geo-backend.onrender.com/api`。
3. 保存并重新部署前端。

### 第 6 步：完成
- 前端网址形如 `https://zhewei-geo-frontend.onrender.com`，发给别人即可打开。
- 以后改代码推 GitHub 自动重部署。

## 路线二（备选）：前端 Cloudflare Pages + 后端 Render
1. 双击本文件夹的 **`构建上线版.cmd`** 生成 `dist`。
2. 注册 https://dash.cloudflare.com/sign-up → Workers & Pages → Create → Pages → Upload assets → 拖入 `dist` → 得 `https://xxx.pages.dev`。
3. 后端按路线一第 3、4 步部署 Render。
4. 前端构建变量设 `VITE_API_BASE` = 后端地址/api。

## 上线前必读
1. 密钥只填云平台环境变量，绝不进代码/仓库（`server\.env` 已 gitignore）。
2. 默认 `SANDBOX_MODE=true`（发布模拟）；真实发布需配平台凭证并设 `SANDBOX_MODE=false`。
3. 配套文件：`render.yaml`、`server/Dockerfile`、`vercel.json`、`server/.env.production.example`、`构建上线版.cmd`。
